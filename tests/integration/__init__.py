"""Integration tests for ReconProbe real-world feature validation.

These tests exercise the actual CLI and pipeline against a local test server,
validating that features work end-to-end rather than in isolated unit tests.
"""
